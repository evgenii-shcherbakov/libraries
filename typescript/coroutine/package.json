{
  "name": "coroutine",
  "version": "1.0.0",
  "description": "Jovy framework (modified by Evgenii Shcherbakov express version)",
  "main": "./dist/index.js",
  "scripts": {
    "test": "nodemon",
    "format": "prettier --write src",
    "build": "rm -rf dist && tsc",
    "mount": "npm run build && npm link",
    "pub": "npm run format && npm run build && npm version patch && npm publish"
  },
  "keywords": [],
  "author": "Evgenii Scherbakov <iipekolict@gmail.com>",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/node": "^18.16.3",
    "nodemon": "^2.0.16",
    "prettier": "2.6.2",
    "typescript": "^4.7.3"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "@types/es6-promise": "^3.3.0"
  }
}
