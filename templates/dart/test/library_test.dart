import 'package:library/library.dart';
import 'package:test/test.dart';

void main() {
  group('Some library tests group', () {
    test('someFunction() test', () {
      expect(someFunction(), 42);
    });
  });
}
